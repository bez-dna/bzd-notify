[package]
name = "bzd-notify-migration"
edition = "2024"

[dependencies]
async-std = { version = "1", features = ["attributes", "tokio1"] }
sea-orm-migration = { version = "1.1.19", features = [
  "runtime-tokio-rustls",
  "sqlx-postgres",
] }
